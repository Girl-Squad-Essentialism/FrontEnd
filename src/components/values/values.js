import React from "react";
import "../../App.css";

const Values = () => {
    const [values, setValues] = useState()

    handleChange = (e) => {
        e.preventDefault()
    }

    // edit values

    // list of  values 

    return (
        <div>

        </div>
    )
}

export default Values;